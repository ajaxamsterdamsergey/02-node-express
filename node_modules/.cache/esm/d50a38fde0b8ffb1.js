let Joi;_e0b‍.x([["LognValidation",()=>LognValidation]]);_e0b‍.w("@hapi/joi",[["default",["Joi"],function(v){Joi=v}]]);

       const LognValidation = async (body) => {
  const result = await Joi.object({
    email: Joi.string().required(),
    password: Joi.string().required(),
  }).validate(body);
  return result;
};
