let Joi;_4c9‍.x([["userValidateMiddleware",()=>userValidateMiddleware]]);_4c9‍.w("@hapi/joi",[["default",["Joi"],function(v){Joi=v}]]);

const userValidSchema = Joi.object({
  email: Joi.string().required(),
  name: Joi.string().required(),
  password: Joi.string().required(),
});

       const userValidateMiddleware = (req, res, next) => {
  const result = userValidSchema.validate(req.body);
  _4c9‍.g.console.log(result.error);
  if (result.error) {
    res.status(403).send("Bad request");
    return;
  }

  next();
};
